# Project intent

Use one NVIDIA GPU as a local AI appliance by serializing access to several specialized inference services behind one stable REST API.

The design optimizes for clarity, VRAM reuse, and inspectability rather than maximum concurrency.
