# Deployment boundary

This repository contains the application and engine definitions only. It does not encode a particular hypervisor, VM identifier, LAN subnet, VPN address, hostname, or home-lab topology.

Run it on a Linux host or VM that can expose an NVIDIA GPU to Docker. Networking outside the Gateway is an operator decision.
